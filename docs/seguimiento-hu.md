# Seguimiento de Historias de Usuario — Proyecto Semestral

Registro vivo de qué HU está hecha, en curso o pendiente, con su rama/PR. Actualizar esta tabla cada vez que se abre o mergea un PR — es la referencia rápida para la demo en clase (el detalle fino de cada feature va en su propio doc, ej. `docs/product-variants.md`).

---

## Convención

- **Rama**: `feat/hu-XXX-nombre-corto`, siempre creada desde `main` actualizado.
- Al mergear un PR, mover la fila a "Completadas" y anotar el PR.
- No reabrir una HU "Completa" solo porque falta contenido/datos — eso es una HU de contenido aparte (ver sección Backlog de contenido).

---

## Completadas (mergeadas a `main`)

| HU | Título | Rama / PR | Responsable | Notas |
|---|---|---|---|---|
| HU-005 | Selector de variantes en la ficha | `feat/product-variants` / PR #98 | Kelvin | Selector completo (tamaño/sabor/conteo/color), precio e imagen reactivos. Falta reseed de categorías restantes (contenido, no bloquea — ver Backlog de contenido). |
| HU-035 | Variante seleccionada persistida en el carrito | `feat/product-variants` / PR #98 | Kelvin | `cartStore`/`CartDrawer` soportan `productId+variantId`. Bug de imagen (no reflejaba la variante en carrito/checkout) corregido en el mismo PR. |
| HU-057 | Página de perfil editable | `feat/hu-057-perfil-editable` / PR #189 | Kelvin | Closes #61. Nombre/foto delegados al modal nativo de Clerk (`openUserProfile()`, ya sincroniza a `User` vía `clerkAuth`). Nueva ruta `/profile`, entrada "Mi perfil" en el menú de cuenta. Se quitó la preferencia de newsletter (`User.preferences.newsletterSubscribed`) que se había agregado inicialmente: no hay ninguna funcionalidad de admin que envíe campañas/novedades filtradas por esa preferencia, así que el campo quedaba guardado pero sin ningún consumidor real. |
| HU-099 | Completar variantes de producto en categorías restantes del catálogo | `feat/variantes-contenido-categorias` / PR #100 | Kelvin | Variantes (con imágenes) puestas en todos los productos del catálogo que las necesitaban. Maquillaje llega a 20/20; el resto de productos por debajo de ese número son de presentación única y no requieren selector de variante. |
| HU-063 | Validación y saneamiento de inputs con Zod | `HU-063-validacion-y-saneamiento-inputs-zod` / PR #101 | Roy | Zod implementado en rutas de backend: productos, carrito, checkout, órdenes, reseñas, webhooks, admin. |
| HU-085 | Búsqueda simple de productos | `feat/hu-085-busqueda` / PR #102 | EiJassiel | Búsqueda por nombre, marca, categoría, descripción corta y necesidad con escapeRegex. Incluye validación Zod. |
| HU-091 | Migración del frontend a TanStack Start (SSR) | `feat/hu-091-tanstack-start` / PR #103 | Kelvin | Migrado a TanStack Router/Start en modo SPA (sin servidor). Rutas de archivo reales con URLs limpias reemplazan el switch de vistas por `?view=...`. Build y Playwright verificados. |
| HU-065 | Logs de auditoría de seguridad | `HU-065-logs-de-auditoria-de-seguridad` / PR #104 | Roy | Modelo `SecurityAuditLog`, middleware de registro y endpoint admin `/admin/audit-logs`. |
| HU-066 | Logging estructurado | `HU-066-logging-estructurado` / PR #105 | Roy | Logger (pino) + `requestLogger` middleware, reemplaza `console.log` por logs estructurados en `index.ts`. |
| HU-067 | Error tracking con PostHog | `HU-067-error-tracking-con-posthog` / PR #107 | Roy | Captura de excepciones (backend y frontend), `ErrorBoundary`, reporte de errores vía PostHog + endpoint admin. Mergeado resolviendo conflicto con HU-065/066 en `backend/src/index.ts` y `package.json`/`bun.lock` (ambas features tocan el bootstrap del servidor). |
| HU-068 | APM y métricas de rendimiento | `HU-068-apm-y-metricas-de-rendimiento` / PR #108 | Roy | Middleware de métricas de performance + endpoint admin `/admin/performance`. |
| HU-083 | Páginas de error (404, 500) | `feat/hu-083-errores` / merge directo a `main` (`ae755a9`) | EiJassiel | `ErrorPage`, `NotFoundView`, `RouteErrorView` y `ErrorBoundary` integrados con TanStack Router. |
| HU-086 | Documentación API (OpenAPI/Swagger) | `feat/hu-086-openapi` / merge directo a `main` (`36b7465`) | EiJassiel | Spec en `/openapi.json` y Swagger UI en `/docs` (solo backend). |
| HU-087 | Refactor del componente admin monolítico | `feat/hu-087-refactor-admin` / merge directo a `main` (`118e928`) | EiJassiel | `AdminApp` dividido en secciones, hooks y componentes; conserva paneles de rendimiento y errores (HU-068/067). |
| HU-082 | Accesibilidad (WCAG 2.1 AA) | `feat/hu-082-accesibilidad` / merge directo a `main` (`015c27c`) | EiJassiel | Skip link, landmarks, `:focus-visible`, modales con focus trap, labels ARIA y `e2e/a11y.spec.ts`. |
| HU-076 | Estrategia de rollback | `feat/hu-076-rollback` / merge directo a `main` (`9d39d30`) | EiJassiel | `docs/rollback-strategy.md`, `tooling/ops/health-check.ps1`, `tag-release.ps1`. |
| HU-075 | Entorno staging | `feat/hu-075-staging` / merge directo a `main` | EiJassiel | `APP_ENV`, `CORS_ORIGINS`, `.env.staging.example` y `docs/staging-environment.md`. |
| HU-078 | CDN Cloudinary imágenes | `feat/hu-078-cloudinary-cdn` / merge directo a `main` | EiJassiel | `frontend/src/lib/cloudinary.ts` + `ProductImage` con fetch transform y lazy load. |
| HU-074 | Backups MongoDB Atlas | `feat/hu-074-backups-mongodb` / merge directo a `main` | EiJassiel | `docs/mongodb-backups.md` + `tooling/ops/mongodb-backup.ps1`. |
| HU-077 | Cache Redis catálogo | `feat/hu-077-cache-redis` / merge directo a `main` | EiJassiel | `backend/src/lib/cache.ts` (Redis o memoria), productos y categorías cacheados. |
| HU-073 | Containerización y CI/CD | `feat/hu-073-containerizacion-cicd` / merge directo a `main` | EiJassiel | `docker-compose.yml`, CI GitHub Actions, Dockerfile puerto 3002, docs. |
| HU-071 | Tests unitarios | `HU-071-tests-unitarios` / PR #119 | Roy | Tests unitarios con Bun y cobertura para filtros de catálogo, variantes/precios efectivos y promociones/descuentos. |
| HU-072 | Tests end-to-end (Playwright) | `HU-072-tests-end-to-end-playwright` / PR #121 | Roy | E2E de checkout con Playwright: catálogo → producto → checkout → Stripe mock; cubre camino feliz, dirección incompleta y error al crear sesión de pago. |
| HU-033 | Precio por variante aplicado en checkout | `feat/hu-033-variant-price-checkout` / PR #134 | Kelvin | Backend valida/recalcula el precio de la variante al confirmar la orden. |
| HU-036 | Variante registrada en las órdenes | `feat/hu-036-variant-in-orders` / PR #135 | Kelvin | `Order.ts` guarda `variantId`/`variantLabel` por línea. |
| HU-032 | Gestión de variantes desde el panel admin | `feat/hu-032-admin-variants` / PR #137, completado en `feat/variant-matrix-admin` / PR #149 | Kelvin | PR #137: editor de variantes admin de una sola dimensión. PR #149: editor "Sabor × Tamaño" completo (matrix, `imagesBySize`, `availableFor`, combo default), **color** como dimensión primaria con swatch picker, endpoint real de subida de imágenes (antes base64), y `priceBySize`/imágenes de combo opcionales con fallback al sabor. Cierra issues #145 y #150. |
| HU-034 | Stock por variante (enforcement) | `feat/hu-034-037-variant-stock` / PR #138, completado en PR #149 | Kelvin | PR #138: enforcement base de stock por variante en checkout. PR #149: `stockBySize` real por combinación sabor+tamaño (antes el stock de un tamaño se compartía entre todos los sabores) — issue #146. |
| HU-037 | Enforcement de stock (inventario) | `feat/hu-034-037-variant-stock` / PR #138 | Kelvin | `decrementStock`/`assertStockAvailable` validan en backend, no solo client-side. |
| — | Servidor MCP (`POST /mcp`) | `feat/mcp-server` | Kelvin | 12 tools de las 24 documentadas en el `.docx` (sección 6) — las que ya tienen HU implementada (MCP ⊆ UI): `catalog.listProducts`/`upsertProduct` (HU-001/016), `variants.upsertVariant`/`updateVariantStock` (HU-032/034), `inventory.adjustStock` (HU-037), `orders.listUserOrders`/`updateOrderStatus`/`getOrderItems` (HU-009/018/036), `users.updateUserRole` (HU-017), `analytics.getSalesReport` (HU-019), `reviews.listReviews` (HU-010), `recommendations.getRelatedProducts` (HU-045). Autenticado con `MCP_SERVICE_TOKEN` (API key de servicio, no Clerk), importable desde Claude Code/Codex/conectores de ChatGPT. `wishlist.getUserWishlist` (HU-044) quedó afuera: la wishlist es solo client-side (localStorage), no hay dato de servidor que exponer. Detalle completo en `docs/mcp-server.md`. |
| HU-061 | Notificaciones en tiempo real (WebSockets) | `feat/realtime-notifications-websockets` / PR #170 | Kelvin | Cierra issue #65. Canal WS sobre Hono/Bun (`createBunWebSocket`), autenticado con token de Clerk en query param (cierre `1008` si es inválido). Centro de notificaciones persistente en el frontend (`NotificationCenter` + `NotificationToaster`), respaldado por la colección `Notification` (Mongo) con fallback a polling REST si el WS no conecta. Admin recibe avisos de stock bajo (venta, ajuste MCP o edición admin, deduplicado por producto/variante/combo) y de nuevas reseñas; cliente recibe pago confirmado y cambio de fulfillment (incluye "enviado"). MCP tool `notifications.broadcast` (`all`/`admins`/`user`). Incluye fix de dedupe de stock bajo: antes solo alertaba una vez y no volvía a avisar si el stock cambiaba a un valor distinto (ej. restock parcial) dentro de la ventana de 6h; ahora dedupea por valor exacto de stock, cualquier cambio dispara alerta nueva. |
| — | Carrito: items clickeables + editor de variante/combinación | `feat/cart-item-click-and-variant-edit` / PR #178 | Kelvin | Cierra issue #177. Extiende HU-035 (variante persistida en el carrito). Click en imagen/nombre de un item navega a `/product/:id` y cierra el drawer. Cada línea con variantes muestra "CAMBIAR" (estilo Taobao/AliExpress): selector inline de sabor+tamaño (matriz) o variante simple, reusando `resolveVariantById`/`sizesFor`/`pickSizeKeepingCurrent`. `cartStore.changeVariant` combina cantidades si la variante destino ya es otra línea del carrito. Incluye fix relacionado: el quick-add "+" de `ProductCard` (catálogo, recién llegados, recomendados) agregaba productos con variantes sin ninguna variante adjunta (`variant: undefined`), dejando líneas sin chip ni forma de editarlas; corregido con `pickDefaultCartVariant`, que resuelve el combo default al mismo id compuesto que una selección real. |
| — | Admin: ordenar por precio/stock, conteo por categoría y columna de reseña | `feat/admin-products-sort-and-rating-column` / PR #180 | Kelvin | Cierra issue #179. Headers "Precio"/"Stock" clicables (asc/desc) en la tabla de Gestión de productos, conteo de productos por categoría en los filtros pill, y columna "Reseña" (Stars + promedio/cantidad vía `/reviews/summary`, agregado en una sola consulta en vez de una petición por producto). Incluye fix de datos: `product.rating`/`reviews` traían números inventados del seed inicial (ej. "2340 reseñas" falsas) nunca reconciliados con la colección `Review` real — se quitaron del seed y `seed-reviews.ts` ahora sincroniza automáticamente; y fix de sort: desempate final por `id` para que "Mejor valorados" (catálogo público) y "Reseña" (admin) siempre coincidan en el mismo orden. |
| — | Admin: filtros, conteos y ordenamiento consistentes en Productos, Pedidos y Clientes | `feat/admin-tables-filters-sort-consistency` / PR #182 | Kelvin | Cierra issue #181. Detectado al revisar el PR #180: Productos, Pedidos y Clientes tenían niveles de funcionalidad distintos. Se nivelan las tres — Productos: filtro Estado (Todos/Activo/Inactivo) con conteo, columna Estado ordenable. Pedidos: conteo en los pills de estado de envío, columnas Total y Fecha ordenables. Clientes: búsqueda por nombre/email (no existía) y columnas Órdenes/Gasto total/Registro ordenables (de paso corrige un bug donde el total de paginación usaba `users.length` en vez de la lista de clientes filtrada). `SortableTh`/`SortClearChip` se movieron a `components/admin/index.tsx` para que las tres tablas compartan el mismo componente. De paso corrige el formato de fecha del admin (Pedidos, Clientes, Errores, ejes de charts): usaban `toLocaleDateString()`/`toLocaleString()` sin locale/timezone, cayendo al formato del navegador (mes/día/año en en-US); nuevo helper `lib/dates.ts` fuerza día/mes/año en America/Panama — con una vuelta extra: el locale `es-PA` de ICU en realidad ordena mes/día/año para el patrón numérico corto (rareza de CLDR propia de Panamá), así que el helper arma el string a mano con `formatToParts` en vez de confiar en el orden automático de `Intl`. |
| — | Footer: acelerar animación de entrada | `fix/footer-animation-speed` / PR #184 | Kelvin | Closes #183. La secuencia de entrada (letras de "Healthora", descripción, iconos sociales, columnas de links, newsletter, barra inferior) tardaba ~2.5s en asentarse del todo. Delays y stagger comprimidos ~45-55%, rigidez (`stiffness`) de los springs subida ~40-60% manteniendo `damping` similar. |
| HU-038 / HU-100 | Envío a domicilio vs retiro en tienda en checkout | `fix/hu-038-100-envio-zona-velocidad` / PR #188 | Kelvin | Closes #42, Closes #185. **Alcance simplificado tras revisión manual**: la primera versión diferenciaba por zona (capital/interior, con autodetección desde la ciudad) × velocidad (estándar/express) — 4 tarifas — pero se revirtió a una sola opción binaria (`shippingMethod: 'delivery' \| 'pickup'`) por agregar demasiada superficie de error/validación. `backend/src/lib/shipping.ts` (espejo en frontend): retiro siempre gratis, envío tarifa plana $6.90 (gratis sobre $50, igual que el comportamiento original pre-feature). `Order` guarda `shippingMethod`/`shippingLabel`/`shippingEta`. Tras revisión manual del feature ya mergeado se agregaron varios fixes de UX en el mismo PR: (1) checkout ya no pide Dirección/Ciudad/Código postal para retiro en tienda — solo nombre+teléfono; `orderAddressSchema`/`requireFullAddress` en el backend hacen esos campos condicionales según `shippingMethod` (checkout, webhooks, fallback de creación de orden, edición de dirección de un pedido). (2) El timeline de retiro salta el paso "Enviada" (3 pasos en vez de 4) y el estado final dice "Listo para retirar" en vez de "Entregada", tanto en `Orders.tsx`/`OrdersSection.tsx` como en las notificaciones push y ambos emails transaccionales. (3) Admin: nueva columna "Envío" con filtro dedicado (Envío a domicilio/Retiro en tienda) en la tabla de pedidos, y la fila de filtros se reorganizó en dos filas con etiquetas "Estado"/"Envío". (4) Admin recibe notificación en tiempo real (`new_order`, sobre el WS de HU-061) cuando se crea un pedido — la tabla de pedidos y las métricas del dashboard se refrescan solas sin recargar la página. (5) Emails: teléfono de soporte a formato panameño (+507), botón "Ver mi pedido" ahora enlaza a `/orders?orderId=<id>` (el link viejo `?view=orders` ya no lo interpretaba nada desde la migración a TanStack Router) y el timeline de pasos del email usa círculos de tamaño fijo (antes "Listo para retirar" deformaba/cortaba el círculo por compartir columna de tabla con la etiqueta larga). De paso: placeholder de teléfono a formato panameño + `lib/phone.ts` (`formatPanamaPhone`) limita el campo a 8 dígitos con guión automático (antes no limitaba nada), aplicado en Checkout, editar dirección de pedido y direcciones guardadas. Tests: `payment-flow.integration.test.ts`, `shipping.test.ts`, `phone.test.ts`, e2e `checkout.spec.ts`. |
| HU-056 | Moderación de reseñas en el admin | `feat/hu-056-moderacion-resenas` / PR #190 | Kelvin | Closes #60. `Review.status` (pending/published/hidden, default published — post-moderación, no pre-aprobación: toda reseña nace publicada, igual que antes). Nueva sección "Reseñas" en el admin: filtrar por estado (solo Todos/Publicada/Oculta — "pending" no se filtra porque ningún flujo lo asigna hoy), aprobar/ocultar/eliminar, búsqueda y filtro por estrellas, paginación server-side. Etiquetas de estado en español (Publicada/Oculta) en vez del valor crudo en inglés. Público (`GET /reviews`, `/reviews/stats`, `/reviews/summary`) y `recomputeProductRating` filtran `status != hidden` (no `== published`, para no ocultar de golpe reseñas ya existentes sin el campo). MCP tool `reviews.moderateReview` (approve/hide/delete) agregado para cumplir el requisito `[MCP]` del issue #60 — verificado end-to-end con una reseña real (creada, vista en la ficha pública, eliminada vía el tool, confirmada fuera de la ficha). Bug encontrado en la misma verificación manual: el filtro `?status=published` del admin usaba igualdad estricta, así que las ~649 reseñas sembradas antes de esta rama (sin el campo `status` guardado en Mongo — el default del schema no se aplica retroactivamente) quedaban invisibles bajo ese filtro aunque sí se mostraban en el cliente. Corregido para tratar "published" como `status != hidden`, igual que el resto de rutas; test de regresión agregado. Segundo bug encontrado al seguir probando: `ReviewsSection.tsx` no tenía ningún control de paginación — con 652 reseñas solo las 20 más recientes eran alcanzables en el admin. Se agregó `PaginationControls` (reusa el componente ya usado en Pedidos/Clientes) con paginación server-side (`page`/`limit` que ya soportaba el backend), búsqueda (por texto de la reseña, autor o nombre de producto, server-side con `escapeRegex`, debounce 300ms) y filtro por cantidad de estrellas (`?rating=`). El Skeleton de carga se disparaba en cada click de filtro (query key cambia por filtro) — se adopta `useOnceLoading` (ya usado en el resto de secciones admin) para mostrarlo una sola vez por sesión. Se conecta al WebSocket de HU-061: `new_review`/`low_stock` invalidan la tabla y el dashboard para refrescar en vivo, igual que `new_order` (HU-038/HU-100). |
| HU-092 | Descuentos automáticos por producto y categoría | `feat/hu-092-descuentos-automaticos` / PR #192 | Kelvin | Closes #126. `backend/src/lib/discounts.ts` resuelve vigencia (fecha inicio/fin) sin cron job — se conecta en `products.ts` (catálogo/ficha) y `resolveVariantPricing` (precio cobrado en checkout, sin tocar `checkout.ts`). Nuevo modal admin "Descuento por categoría" + inputs de `priceBefore`/vigencia en `ProductModal` (antes `priceBefore` no era editable desde la UI). **Fix agregado antes de mergear** (reportado en pruebas manuales — el descuento se guardaba pero no se veía en cliente): el cache del catálogo público (`backend/src/lib/cache.ts`, HU-077) nunca se invalidaba al crear/editar/borrar un producto ni al aplicar/quitar un descuento por categoría, así que servía la respuesta vieja hasta que el TTL (60s) expiraba solo. Se agregó `clearCatalogCache()` en esos puntos. De paso se agregó `priceBefore`/`discountStartsAt`/`discountEndsAt` también a nivel de **variante simple** (antes solo existía a nivel de producto, así que un producto con variantes no podía tener descuento propio por variante) y `apply-category`/`remove-category` se extrajeron a `applyCategoryDiscount`/`removeCategoryDiscount` en `discounts.ts` para reusar esa misma lógica de variante. |
| — | Admin: banear autor de una reseña para un producto especifico | `feat/hu-194-banear-autor-resena` / PR #195 | Kelvin | Closes #194. Reportado tras probar HU-056: "Eliminar" solo borraba el documento — el mismo usuario podía volver a publicar exactamente el mismo comentario en el mismo producto, ya que no quedaba ningún registro de que ya había comentado ahí. Nuevo modelo `ReviewBan` (índice único `productId+userId` — el baneo es por producto, no una suspensión global de la cuenta). `POST /admin/reviews/:id/ban` banea al autor de esa reseña para ese producto y la elimina en la misma acción; `POST /reviews` (creación pública) rechaza con 403 si el usuario está baneado para ese producto. `GET /admin/reviews/bans` / `DELETE /admin/reviews/bans/:id` para listar y revertir baneos (no es una acción sin vuelta atrás). Admin: botón "Banear autor" junto a Aprobar/Ocultar/Eliminar en cada fila, y panel "Usuarios baneados" en el header de la sección. |
| — | Ficha de producto: filtrar reseñas por cantidad de estrellas | `feat/hu-196-filtro-estrellas-resenas` / PR #197 | Kelvin | Closes #196. Sugerido en la misma sesión que HU-194: el desglose por estrella en `ReviewSection.tsx` ya calculaba el conteo por cada valor pero las filas eran solo visuales. Cada fila (5★...1★) ahora es un botón que filtra la lista de reseñas a esa calificación (toggle — un segundo click o "Ver todas" lo quita); 100% client-side, sin cambios de backend, ya que las reseñas del producto ya están todas cargadas vía `useReviews`. El desglose sigue mostrando los totales sin filtrar. Verificado en navegador con Playwright contra un producto real con reseñas mixtas. |
| HU-039 | Cálculo de ITBMS (Panamá) | `feat/hu-039-itbms` / PR #202 | Kelvin | Closes #43. El checkout ya aplicaba 7% plano sobre todo el subtotal (`checkout.ts`) sin exención — faltaba la exención de ley para medicamentos/artículos de salud. Nuevo `Product.taxExempt`, **derivado automáticamente de la categoría** (`isTaxExemptCategory` en `backend/src/lib/tax.ts`, `true` solo para "Medicamentos") vía hooks `pre('save')`/`pre('findOneAndUpdate')` en `Product.ts` — no es un checkbox editable en el admin (se probó con un toggle manual y se descartó: el objetivo era que fuera automático por categoría, sin intervención humana). `computeItbms` (espejado en `frontend/src/lib/tax.ts`) calcula el 7% solo sobre líneas no exentas, con el descuento del carrito aplicado proporcionalmente. Labels "Impuestos"/"Impuesto (IVA)" renombrados a "ITBMS" (Checkout, CartDrawer, Orders, panel Ganancias admin). |
| HU-042 | Seguimiento de envío (tracking) | `feat/hu-042-tracking-envio` / PR #203 | Kelvin | Closes #46. No existía forma de registrar ni ver carrier/número de guía de un envío. Nuevo `Order.carrier`/`Order.trackingNumber` (opcionales, solo delivery). `backend/src/lib/tracking.ts` (espejado en frontend): catálogo de couriers conocidos en Panamá (DHL, UPS, FedEx, Correos de Panamá, mensajería propia) con plantilla de URL de rastreo pública — carrier fuera de la lista se guarda como texto libre sin enlace. Sin integración real con couriers (no hay cuentas comerciales/API contratadas aún — evaluado y descartado por ahora, ver discusión de la sesión): al pasar un pedido a "enviado" sin courier/número ya asignados, `adminOrders.ts` genera un número de guía simulado (`generateTrackingNumber`, formato `HLT-XXXXXXXX`) y asigna courier "Mensajería Healthora" por defecto — evita fabricar números falsos de DHL/UPS que no resolverían en sus sitios. Ese courier/número ahora también se muestra en el email de actualización de estado (`sendOrderStatusUpdateEmail` en `email.ts`) cuando el estado pasa a "enviado". Catálogo de couriers reducido a solo "Mensajería Healthora" (`CARRIERS` en `tracking.ts`, backend y frontend) — se quitaron DHL/UPS/FedEx/Correos de Panamá del selector del admin ya que ningún envío sale realmente por esos couriers todavía. Cliente ve courier + N° de guía bajo el timeline de estado en `Orders.tsx`. El editor manual inicial (`PATCH /admin/orders/:id/tracking` + input en `OrdersSection.tsx`) se quitó de la UI del admin una vez confirmado que el número se asigna solo al marcar "enviado" — esa columna ahora es de solo lectura, el endpoint backend queda disponible pero sin ningún llamador en la UI. |
| HU-200 | Ocultar cantidad exacta de stock salvo cuando es baja | `fix/hu-200-ocultar-stock-exacto` / PR #201 | Kelvin | Closes #200. `ProductDetail.tsx` y la fila "Stock" de `Compare.tsx` mostraban el número exacto de unidades sin importar el volumen. Nuevo `frontend/src/lib/stock.ts` (`LOW_STOCK_DISPLAY_THRESHOLD = 10`, `isLowStock`) centraliza el umbral, separado del `LOW_STOCK_THRESHOLD` del backend (ese es para alertar al admin de reabastecer, no para mostrarle nada al cliente). 3 niveles: `AGOTADO` (0), `SOLO QUEDAN {n}` en coral (`n ≤ 10`), `EN STOCK` sin número (el resto). Extendido en la misma rama a `ProductCard.tsx` (grid de catálogo/recomendados/vistos recientemente) tras revisión manual — el badge de stock bajo se apila junto al tag existente (Más vendido/Nuevo) en la esquina superior izquierda en vez de reemplazarlo; `Agotado` sigue siendo excluyente. Verificado en navegador (Playwright) contra un producto real con stock=1 en las tres superficies (card, ficha, comparador). |
| HU-041 | Devoluciones y reembolsos | `feat/hu-041-devoluciones-reembolsos` / PR #193 | Kelvin | Closes #45. Modelo `Return` (requested→approved→in_transit→refunded, o rejected), ventana de 30 días desde `order.createdAt`. Admin reembolsa vía Stripe Refunds API real sobre el `payment_intent` de la orden; cada cambio de estado envía email. **Ajuste tras mergear `main`** (esta rama se abrió antes de HU-038/HU-100 — envío a domicilio vs retiro en tienda — y no conocía `shippingMethod`): nuevo `Return.returnMethod` (`courier_pickup` \| `store_dropoff`), derivado del `shippingMethod` de la orden al crear la solicitud (un pedido de retiro en tienda nunca tuvo mensajero de ida, así que tampoco lo tiene de vuelta). Para `store_dropoff` el flujo admin salta `in_transit` (aprobada → reembolsada directo, el cliente trae el producto en persona); para `courier_pickup` se mantienen los 4 estados igual que antes. Copy de email/notificación in-app también varía por método en el estado `approved` ("un mensajero pasará a recoger" vs "puedes traer el producto a la tienda"). También se integró la campana de notificaciones en tiempo real de HU-061 (antes esta rama solo mandaba email): `return_requested` avisa a los admins al crear una solicitud, `return_status` avisa al cliente en cada cambio de estado. Requirió un merge de `main` (46 commits de por medio) con conflictos resueltos en `Notification.ts`, `index.ts`, `api.ts`, `AdminPanel.tsx`/`types.ts` y `components/admin/index.tsx` (todos por líneas adyacentes entre esta rama y HU-038/HU-100/HU-056, sin conflicto real de lógica). **Reemplazo en vez de reembolso** (caso "llegó el producto equivocado/dañado", pedido durante la misma sesión): nuevo estado `replaced` en `Return.status` (junto a `refunded`), con `Return.replacementOrderId` apuntando al pedido creado. `lib/returns.ts` agrega `createReplacementOrder`: reconstruye las líneas completas (precio, variante, imagen) cruzando `returnDoc.items` contra las líneas del pedido original (que sí las tiene completas), crea un `Order` nuevo a costo $0 (`subtotal`/`tax`/`shipping`/`total: 0`, `paymentStatus: 'paid'` ya que no hay nada que cobrar, `fulfillmentStatus: 'unfulfilled'` para que el equipo lo prepare como cualquier pedido normal, mismo `shippingMethod`/dirección que el original) y descuenta stock real (`decrementStock`) + dispara aviso de stock bajo, igual que el webhook de pago. Nuevo `Order.replacesOrderId` para trazabilidad. En el punto donde antes había un solo botón "Reembolsar", ahora el admin ve dos: "Reembolsar" y "Reenviar producto correcto" — ambos disponibles una vez que el producto físicamente volvió (`in_transit` para `courier_pickup`, `approved` para `store_dropoff`). Nueva columna "Ubicación" en la tabla de Devoluciones (dirección de entrega original, solo para `courier_pickup`, tomada de `Return.pickupAddress` — snapshot guardado al crear la solicitud para no tener que hacer join con `Order` en el listado). **Corrección de diseño pedida en la misma sesión**: quién resuelve reembolso vs reemplazo no debe ser el admin a su criterio, sino el cliente al pedir la devolución. Nuevo `Return.desiredResolution` (`refund` por defecto, o `replacement`), elegido por el cliente en el formulario de solicitud (`Orders.tsx`, selector "¿Qué prefieres?") y guardado en la creación. La tabla admin muestra "Prefiere: reembolso/reenvío" por fila, y de los dos botones de resolución el que coincide con lo pedido queda resaltado como principal con sufijo "(solicitado)" — el admin conserva la otra opción disponible solo como excepción operativa (ej. sin stock para reenviar). **Tercera ronda de feedback**: (1) reembolso confiaba ciegamente en la respuesta síncrona de `stripe.refunds.create` en vez de esperar confirmación real de Stripe — nuevo estado `refund_pending` (interno, no seteable por el admin) mientras se espera el webhook `refund.updated` (mismo patrón que `checkout.session.completed` para el pago); `lib/returns.ts#confirmReturnRefund` marca `refunded` (+ email/notificación) si Stripe confirma `succeeded`, o revierte a `in_review` + avisa a los admins si `failed`/`canceled`. Test nuevo en `payment-flow`-style simula el webhook contra `/webhooks/stripe`. (2) `StatusPill` en Devoluciones mostraba el enum crudo en inglés (`REQUESTED`, `IN_TRANSIT`) en vez de `STATUS_LABELS`; corregido pasando el label ya traducido, con las entradas de color correspondientes agregadas a `STATUS_COLORS`. (3) Se quitó el botón/link alternativo de resolución (reembolsar vs reenviar) — el admin ya no "elige" nada en ese paso, solo ejecuta `desiredResolution` con un único botón. A cambio se agregó un paso `in_review` ("Producto recibido, en revisión") entre que el producto físicamente vuelve (`in_transit` para `courier_pickup`, o al llegar el cliente para `store_dropoff`) y la resolución — el admin confirma que inspeccionó el producto antes de reembolsar/reenviar, en vez de resolver a ciegas apenas se marca en tránsito. Bug de layout de paso: `Card` (componente admin compartido) no le daba padding al `title`/`sub` cuando `pad={0}` (patrón usado por toda sección con tabla), dejando el título pegado al borde — corregido en el componente, beneficia también a Errores/Rendimiento que tenían el mismo problema latente. **Cuarta ronda de feedback (cierre/merge)**: un pedido de reemplazo (el envío gratis en sí, `replacesOrderId`) no puede pedir otra devolución — botón "Solicitar devolución" oculto en `Orders.tsx` y validación espejo en `POST /returns` (400 si `order.replacesOrderId`). Contador "Devoluciones" del sidebar admin no se actualizaba al llegar una notificación `return_requested` en vivo (invalidaba la lista pero no `admin-returns-count`) — agregado a `INVALIDATION_BY_TYPE` en `NotificationsRealtime.tsx`. Columna "Pago" de Pedidos ahora cruza cada orden con su `Return` (por `orderId`) para reflejar el estado real: una devolución de **reembolso** en curso muestra su propio estado (Solicitada/Aprobada/.../Reembolsada) en vez de "Pagado" a secas, porque ahí sí hay dinero en juego; una devolución de **reemplazo** en cambio nunca toca el pago de esa orden (el cliente recibe el mismo producto corregido, no su dinero de vuelta), así que se muestran **ambos** badges apilados — "Pagado" + "Reemplazo en camino"/"Reemplazo en tienda" — para que quede claro que el pago no se tocó pero hay un reemplazo activo; el pedido de reemplazo en sí ($0.00) muestra "Sin costo". Filtro dedicado "Pago" (Todos/Pagado/Pendiente/Cancelado/Reembolsado/Devolución en curso/Reemplazo/Sin costo) agregado junto a Estado/Envío. Columna "Productos" de Pedidos ampliada (`minWidth` en vez de `table-layout: auto` dejándola aplastarse a ~90px). Copy de "Reemplazo en tienda" (email + notificación + panel de pedidos) decía "ya estamos preparando... te avisaremos cuando esté listo" — corregido a "ya está listo para recoger", porque un reemplazo en tienda nace directo en `fulfillmentStatus: 'delivered'` (= listo para retirar), sin ningún paso de preparación de por medio. `seed-orders.ts` no limpiaba `Return` al resembrar `Order` (`deleteMany`), dejando devoluciones huérfanas apuntando a pedidos borrados — agregado `Return.deleteMany({})` junto al de `Order`. **Reconciliación de historia al mergear**: la rama remota (`origin/feat/hu-041-devoluciones-reembolsos`, base del PR #193) había sido reescrita por fuera de esta sesión con una implementación alternativa co-autoreada con Cursor (`backend/src/lib/returnAdmin.ts` + MCP tool `returns.approveReturn`), divergiendo del historial local desde el primer commit. Se descartó esa versión (el reembolso ahí seguía confiando en la respuesta síncrona de Stripe, el bug corregido en la tercera ronda arriba) a favor de esta rama — force-push sobre el PR tras mergear `main` (HU-040/HU-048) tras reconciliar. Sigue sin existir MCP tool para devoluciones. |
| HU-055 | Alertas de stock bajo (umbral por producto) | `feat/hu-055-alertas-stock-bajo` / PR #204 | Kelvin | Closes #59. El umbral de "stock bajo" era un único valor global (env var, default 5) para alertas en tiempo real (HU-061) y el widget del dashboard, sin forma de ajustarlo por producto. Nuevo `Product.lowStockThreshold` (opcional); `scanAndNotifyLowStock` resuelve `opts.threshold ?? product.lowStockThreshold ?? global`, así que ningún call site existente necesitó cambios. Dashboard: query con `$expr`/`$ifNull` en vez de `$lte` fijo. Admin: input de umbral en `ProductModal`, tabla de productos resalta en coral + badge "Bajo" los productos activos bajo su umbral efectivo. Umbral por **variante** individual queda fuera de alcance (mismo criterio que otros campos a nivel de producto, ej. `taxExempt`); el gap de detección por combo en el dashboard es el issue #153 separado, no tocado aquí. |
| — | Dashboard: "Stock bajo" detecta combos críticos, no solo el total del producto | `fix/dashboard-stock-bajo-combos` / PR #215 | Kelvin | Closes #153. El widget leía `Product.stock`, el campo denormalizado que es la suma de todas las combinaciones — un producto con 5 combos de 20 unidades (total 100) nunca aparecía en "stock bajo" aunque uno de esos combos estuviera en 0. Reusa `enumerateStockCells` (`lib/lowStock.ts`, ya usado por las alertas en tiempo real de HU-061) para evaluar cada celda de stock por separado (producto sin variantes, variante simple, o combo sabor/color × tamaño), respetando `lowStockThreshold` por producto (HU-055). El KPI "Existencias bajas" tenía el mismo bug arrastrado en sentido inverso: contaba solo los primeros 6 resultados (el `.limit(6)` de la query vieja) en vez del total real de celdas críticas — corregido a la vez. Verificado manualmente contra datos reales: 2 celdas críticas del mismo producto ("choco · 2" en 0, "pato · 5" en 3) ahora aparecen como dos filas distintas con su combo y stock real, en vez de una sola fila con el total sumado del producto. |
| — | Ventas: top variantes vendidas (combos sabor/color × tamaño) | `feat/ventas-top-variantes-vendidas` / PR #216 | Kelvin | Closes #154. Los aggregates de `adminSales.ts` (topProducts, topCategories, topBrands, revenueByCategory) agrupaban por producto/categoría/marca vía `items.productId`, ignorando `items.variantId`/`items.variantLabel` que ya vienen poblados en cada orden real y en el seed de pedidos ficticios — para productos con matrix sabor×tamaño la sección Ventas no podía responder "¿qué combo se vende más de un mismo producto?". Nuevo aggregate `topVariants` agrupado por producto+`variantLabel` (mismo patrón `$unwind`/`$group`/`$sort` que los existentes), mostrado en nuevo card "Top variantes vendidas" en `SalesSection.tsx`. |
| — | Admin Pedidos: buscador no encontraba órdenes por nombre de producto | `fix/pedidos-buscar-por-producto` / PR #221 | Kelvin | Closes #220. Reportado en pruebas manuales del PR #216 (Ventas: top variantes vendidas): buscar "Men's Multi Gummies" en Pedidos daba "0 resultados de 182" pese a que Ventas confirmaba 9 unidades vendidas. Causa: el filtro de búsqueda (`useAdminPanel.ts`) solo comparaba el término contra `order._id`/`customerName`/`customerEmail` — nunca contra `items[].productName`/`variantLabel`, aunque el placeholder no lo prometía tampoco ("Buscar por ID, cliente o email…"). Nueva `orderMatchesSearch()` extiende el match a los productos/variantes de cada línea (el backend ya trae `items` completo sin paginación, no requirió cambios de API); placeholder actualizado a "...o producto". Nota UX confirmada con el usuario: el match sigue siendo por substring (igual que el resto del buscador), así que buscar "Men's Multi Gummies" también trae "Women's Multi Gummies" ("men's" es substring de "women's") — comportamiento aceptado, no un bug. |
| — | Migración de imágenes a Cloudinary (upload real + catálogo completo) | `feat/cloudinary-image-migration` / PR #217 | Kelvin | Closes #159. El upload del admin (`imageStorage.ts`, compartido con fotos de devolución y la MCP tool de imagen de variante) escribía a disco local del backend y armaba la URL con `getPublicBackendUrl()` — en producción (Koyeb) el disco es efímero, y si se subía desde un backend local la URL `localhost` quedaba guardada en el Mongo compartido con producción (ya había roto 2 productos reales antes de esta rama). Se reemplazó por upload real a Cloudinary. **Bug de SDK encontrado en el camino**: el paquete oficial `cloudinary` (Node) dejaba de firmar los requests después del primer upload exitoso corriendo bajo Bun — reproducido de forma determinística con concurrencia 1 y 8, con y sin delay entre llamadas, siempre falla desde el segundo request en adelante con "Upload preset must be specified when using unsigned upload"; un POST firmado a mano con `curl` contra la misma cuenta no tenía el problema, así que el bug es del SDK/runtime, no de la cuenta ni de las credenciales. Se descartó el SDK y se implementó `backend/src/lib/cloudinaryUpload.ts`: upload firmado a mano (SHA1 sobre los params + `api_secret`, `fetch` + `FormData`), sin dependencias nuevas. Aprovechando que ya había cuenta de Cloudinary configurada (antes solo usada para el fetch-transform de HU-078, nunca con cloud name seteado en la práctica), se migraron también las ~3325 imágenes reales del catálogo (`frontend/public/products/`, se mantienen commiteadas en el repo como respaldo) reescribiendo las referencias en `seed.ts` — incluyendo tres patrones que armaban la ruta dinámicamente en vez de como string literal (el helper de sabor×tamaño de productos fitness, el fallback de imagen por defecto para productos sin `imageUrl` propio, y ~154 `.map()` de imágenes por tamaño/sabor repetidos en variantes), detectados recién al probar en el navegador real cuando el fetch-transform de Cloudinary devolvía 401 para las URLs que seguían siendo locales. Encontrados y migrados de la misma forma, en una segunda pasada tras revisión manual del usuario: 3 imágenes de iHerb (`cloudinary.images-iherb.com`) y 8 de NatureMade/Shopify que también fallaban — la cuenta de Cloudinary bloquea el fetch-transform a cualquier dominio externo (401), no solo a esos dos; verificado con un barrido de las 2853 URLs de imagen únicas de los 200 productos, cero quedan fuera de la cuenta propia. `frontend/src/lib/cloudinary.ts` (`optimizeImageUrl`) actualizado para además optimizar (`f_auto,q_auto,w_`) las imágenes ya subidas a la cuenta propia insertando la transformación en la delivery URL, no solo las externas vía fetch — la guarda anterior (`if (url.includes('res.cloudinary.com')) return url`) las hubiera dejado siempre a tamaño completo. Removidos `getPublicBackendUrl` y la ruta estática `/uploads` + su bypass de CORP (sin más callers/uso tras el fix). Requiere `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET` en el entorno de Koyeb (producción) para que el upload del admin funcione ahí — configurado manualmente tras el merge, no viaja con el deploy. |

## En curso

| HU | Título | Rama | Responsable | Notas |
|---|---|---|---|---|
| HU-080 | Optimización de imágenes (Cloudinary, lazy load) | `HU-080-optimizacion-imagenes-cloudinary-lazy-load` / PR #124 | Roy | `srcset`/`sizes` responsive con Cloudinary `f_auto,q_auto`, lazy por defecto y prioridad selectiva para LCP. Issue #84 cerrada; pendiente merge del PR a `main`. |
| — | Descuento por categoría: aplicar también a combinaciones sabor/color×tamaño (matrix) | `fix/hu-092-descuento-categoria-matrix` / PR #206 | Kelvin | Closes #205. Reportado en pruebas manuales: no se había verificado a mano el flujo de HU-092 hasta ahora y el modal "Descuento por categoría" saltaba por completo los productos en modo "Variante × Tamaño" (`"N con variante×tamaño no se tocaron"`). Nuevo `priceBeforeBySize` (paralelo a `priceBySize`/`stockBySize`) guarda el precio "antes" de cada combinación; `applyCategoryDiscount`/`removeCategoryDiscount` (`backend/src/lib/discounts.ts`) aplican y revierten el descuento por combo respetando `availableFor`, usando la vigencia del sabor/color (no hay fecha por combo). Bug relacionado encontrado en el camino: `resolveVariantPricing` nunca leía el override `priceBySize` de un combo al calcular el precio cobrado en checkout — solo sumaba `primary.price + size.price`, así que el precio mostrado en la ficha (que sí lee `priceBySize`) podía no coincidir con el cobrado. Trampa de Mongo encontrada al revertir: `{'variants.priceBeforeBySize': {$ne: null}}` no matcheaba nada porque un tamaño (que nunca tiene ese campo) cuenta como "null" para `$ne` sobre arrays — se cambió a `$elemMatch`. Botón "Descuento por categoría" del header con `white-space: nowrap` (se partía en 3 líneas). **Iteración tras probar en el navegador real** (issue reportada como "se queda bugeado en Aplicando..." resultó ser un hot-reload de `bun --watch` a mitad de request, no un bug — reproducido aparte contra los 20 productos reales de "Fragancias" vía MCP, sin colgarse): se agregó `categoryDiscount` (booleano, en producto/variante/primary) que marca qué se descontó desde esta herramienta masiva. `removeCategoryDiscount` ahora solo revierte lo marcado — un descuento puesto a mano en el editor individual de un producto (o precargado en el seed) ya no se borra al quitar el descuento de toda la categoría. Nuevo indicador en el propio modal, ahora como chips clicables (mismo estilo que los filtros de categoría de la tabla) en vez de una línea de texto plano, listando qué categorías ya tienen un descuento masivo corriendo — click en un chip selecciona esa categoría en el selector. Segunda ronda de pruebas manuales: el descuento por categoría, al tocar una variante/combo que ya tenía un descuento hecho a mano en su propio editor, usaba el "precio antes" de ese descuento manual como base en vez del "precio" (precio realmente cobrado) — por ejemplo, con $150→$130 manual y un descuento masivo de $10, daba $140 en vez de los $120 esperados ($10 sobre el precio real de $130) — y al revertir el descuento por categoría, ese descuento manual quedaba borrado del todo en vez de restaurado. Causa: `applyCategoryDiscount` usaba `priceBefore ?? price` como base indiscriminadamente, pensado solo para no compensar al reaplicar el mismo descuento masivo, pero esa misma regla se activaba incorrectamente sobre un `priceBefore` puesto a mano. Nuevo campo `categoryDiscountRestore`/`categoryDiscountRestoreBySize` (snapshot de `{price, priceBefore, vigencia}` tomado la primera vez que la herramienta masiva toca un ítem): la base ahora es ese snapshot solo cuando se re-aplica sobre un descuento que ya es de esta herramienta (`categoryDiscount` ya en `true`) — la primera vez, siempre usa `price` (el precio real vigente), preservando cualquier descuento manual previo en el snapshot para restaurarlo tal cual al revertir. Bug de orden encontrado al escribir las pruebas: para un producto matrix con dos tamaños del mismo sabor, el segundo combo procesado en la misma llamada veía `categoryDiscount` ya en `true` (mutado por el primer combo dentro del mismo loop) y se saltaba su propio snapshot — corregido cacheando el estado "ya era de esta herramienta" de cada sabor una sola vez, antes de procesar sus combos. Tercera ronda: un descuento manual hecho *después* de que un producto/combo ya había pasado por el descuento por categoría no se mantenía — al guardar desde el editor individual, `categoryDiscount`/`categoryDiscountRestore(BySize)` se re-enviaban sin cambios (el editor no tiene checkbox para tocarlos, se preservan para no perderse en un guardado no relacionado), así que un "quitar descuento por categoría" posterior revertía más allá de ese descuento manual nuevo, hasta el snapshot viejo de antes del *primer* descuento masivo. Nueva `clearStaleCategoryDiscountMarkers` (`backend/src/lib/discounts.ts`), llamada en `PUT /admin/products/:id` comparando contra el producto actual en la base: si el precio/precio-antes que llega realmente cambió respecto a lo guardado, limpia `categoryDiscount` y su snapshot ahí mismo — así ese nuevo descuento manual queda protegido de un "quitar descuento" posterior. Un guardado que no toca esos campos (edición de un campo no relacionado) deja la marca intacta. Cuarta ronda: no había forma de poner un descuento manual a una combinación desde el editor individual (el editor "Variante × Tamaño" nunca tuvo campos para esto). Nuevo input "Precio antes" por celda de combinación (junto a "Precio"/"Stock" ya existentes) en `ProductVariantsMatrixEditor.tsx`, y "Vigente desde/hasta" a nivel de sabor/color (aparece solo si alguna de sus combinaciones activas ya tiene un "precio antes" puesto — la vigencia es compartida por sabor, no por combinación individual, igual que ya lo hace el descuento por categoría). `clearStaleCategoryDiscountMarkers` extendida para también detectar un cambio de solo `priceBeforeBySize` (antes solo miraba `priceBySize`), ya que ahora el admin puede editar el "precio antes" de una combinación sin tocar su precio. |

## Pendientes — nueva funcionalidad (sin iniciar)

_(ninguna en este momento)_

## Pendientes — asignadas a Roy (infra, sin tocar código de producto/variantes)

Ramas secuenciales desde `main` (una a la vez, todas tocan `backend/src/index.ts`, mergear una antes de abrir la siguiente):

| Orden | HU | Título |
|---|---|---|
| 1 | HU-062 | Rate limiting |
| 2 | HU-064 | Security headers (CSP, HSTS, X-Frame) |
| 7 | HU-069 | Alertas y monitoreo de uptime |

HU-065/066/067/068 ya mergeadas a `main` (ver tabla de Completadas), fuera de orden respecto al plan original (Roy las abrió antes que 062/064) — 062 y 064 siguen pendientes.

En pausa hasta que variantes/UI se estabilicen: HU-081 (HTTP cache — toca API/backend).

---

## Nota: drift de numeración HU (detectado 2026-07-09)

El `.docx` fuente (`deliverables/Healthora-Historias-de-Usuario.docx`) tiene, desde la sección 3.5 en adelante, dos historias nuevas insertadas (HU-061 "Suscripción de reposición automática" e HU-062 "Recordatorio de recompra", issues #95/#96) que corrieron +2 la numeración de todo lo posterior **en el documento**. Ese corrimiento nunca se propagó a este archivo ni a los títulos de issues ya creados/cerrados, así que hay dos numeraciones en paralelo para el mismo trabajo:

| Feature | Número usado aquí / en el issue | Número actual en el `.docx` |
|---|---|---|
| Notificaciones en tiempo real (WebSockets) | HU-061 (issue #65, PR #170) | HU-063 |
| Rate limiting (pendiente, Roy) | HU-062 | HU-064 |
| Validación y saneamiento Zod | HU-063 (PR #101) | HU-065 |
| Security headers (pendiente, Roy) | HU-064 | HU-066 |
| Logs de auditoría de seguridad | HU-065 (PR #104) | HU-067 |
| Logging estructurado | HU-066 (PR #105) | HU-068 |
| Error tracking PostHog | HU-067 (PR #107) | HU-069 |
| APM y métricas | HU-068 (PR #108) | ~HU-070 (no confirmado) |
| Alertas y monitoreo de uptime (pendiente, Roy) | HU-069 | ~HU-071 (no confirmado) |

No se renumeró nada de lo ya mergeado (bajo valor, alto riesgo para trabajo con historial/PRs ya cerrados). Si se quiere resolver de raíz, decidir entre: (a) renumerar el `.docx` para que HU-061/062 nuevas se muevan al final (ej. HU-101/102) y todo lo demás vuelva a su número original, o (b) adoptar la numeración nueva del `.docx` y actualizar las filas de esta tabla + títulos de issues abiertos de Roy. Mientras tanto, usar el número de **issue de GitHub** (no el HU-XXX) como identificador sin ambigüedad.

## Bugs abiertos

_(ninguno pendiente por ahora)_

## Bugs resueltos

- ~~Imagen del item en el carrito no refleja la variante seleccionada~~ — `CartDrawer.tsx` y `Checkout.tsx` llamaban `<ProductImage product={it.product} .../>` sin pasar la imagen de `it.variant`, así que siempre caía al fallback (imagen del producto o variante `isDefault`). Se corrigió pasando `imageUrl={it.variant?.images?.[0] ?? it.variant?.imageUrl}` en ambos. De paso, `Checkout.tsx` también mostraba precio y brand por línea ignorando la variante — corregido a la vez.
- ~~Orden no se crea ni llega email tras pago exitoso de Stripe~~ (Issue #106, PR #109) — tras la migración a TanStack Start (HU-091) las rutas pasaron de `?view=...` a paths reales (`/checkout`, `/success`), pero `success_url`/`cancel_url` en `backend/src/routes/checkout.ts` seguían apuntando a `${origin}/?view=success...`. El router nuevo ignora `view` en `/` y renderiza Landing, así que el componente `Success` (crea la orden de respaldo si el webhook no llegó, manda el correo, limpia el carrito) nunca se montaba. Se corrigió apuntando a `/success` y `/checkout`. De paso se arregló que `GET /orders?stripeSessionId=` devolvía 200 con `{error}` en vez de 404 cuando la orden de respaldo aún no existía, así que el retry de `react-query` nunca se activaba.
- ~~Imagen de la variante no se reflejaba en pedidos/email + "volver a comprar" perdía la variante~~ (Issue #150, PR #149) — `buildPaidLineItem` usaba siempre la foto a nivel de producto en el line item pagado; `addItemImages()` en `orders.ts` además sobreescribía en cada lectura de pedidos el `imageUrl` ya correcto con la foto genérica; y `Orders.tsx` `handleReorder` agregaba el producto al carrito sin la variante. Corregido con `resolveVariantImage()` (prioriza `imagesBySize` del combo → imágenes del sabor → del tamaño → fallback) y `resolveVariantById()` para reconstruir la variante/combo desde el `variantId` persistido.
- ~~`availableFor` vacío (combinación desactivada) se trataba como "sin restricción"~~ (PR #149) — el chequeo `!v.availableFor?.length` es `true` tanto si el campo no existe como si es un array vacío, así que una combinación sin ningún tamaño activo terminaba disponible para todos los sabores. Persistir esto además chocaba con que Mongoose defaultea campos array a `[]` cuando no vienen en el payload, colapsando "sin restricción" (ausente) y "restringida a nadie" (vacío) en el mismo valor guardado. Corregido en `sizesFor`, `decomposeToMatrix`, `composeFromMatrix`, `computeTotalStock` y el schema de Mongoose (`default: undefined`).
- ~~Stock y precio totales (lista de admin, dashboard de stock bajo, ficha de producto) ignoraban overrides por combinación~~ (PR #149) — `sumVariantStock`/`computeTotalStock` sumaban solo el stock base de los tamaños, no `stockBySize`; `effectivePrice` en la ficha no leía `priceBySize`. Corregido para que ambos se calculen a partir de las combinaciones reales.
- ~~Modal de editar/crear producto reseteaba el scroll de la página al abrir o cerrar~~ (PR #149) — el modal sincroniza su estado con la URL vía TanStack Router (`useSearchParamsCompat` → `navigate()`), y el router tiene `scrollRestoration: true`, que resetea el scroll en cualquier navegación aunque sea solo un cambio de query params. Corregido con `resetScroll: false` en esa navegación.
- ~~Cambiar de sabor reseteaba el tamaño seleccionado a la primera opción~~ (Issue #172, PR #173) — en productos con matriz sabor×tamaño (ej. Serious Mass Weight Gainer), los handlers de cambio de sabor en `ProductDetail.tsx` llamaban a `pickDefaultSize` incondicionalmente. Corregido con `pickSizeKeepingCurrent` (`productVariants.ts`), que conserva el tamaño ya elegido si sigue disponible para el nuevo sabor (respetando `availableFor`) y solo cae al default si no lo está.
- ~~Buscador del header (cliente) no aceptaba texto: se borraba en cada tecla~~ (Issue #174, PR #175) — `useSearchParamsCompat.ts` construía un `URLSearchParams` nuevo en cada render sin memoizar. `Header.tsx` sincroniza su estado local `search` con un `useEffect([searchParams])`, así que cada tecla (`onChange` → `setSearch` → re-render → nueva referencia de `searchParams`) disparaba ese efecto y reseteaba el input a `""` antes de que la URL cambiara — el buscador de cliente era literalmente inutilizable. Corregido con `useMemo` sobre `location.searchStr`, para que la referencia solo cambie cuando el query string real cambia.
- ~~`SMTP_FROM` con dominio distinto a la cuenta autenticada (señal de spam)~~ (Issue #171, PR #176) — `SMTP_FROM` caía por defecto a `noreply@healthora.com`, un dominio distinto a `SMTP_USER` (`healthora24@gmail.com`), señal clásica de spoofing/spam. Corregido con `getSmtpFrom()` (`lib/email.ts`), que cae a `Healthora <SMTP_USER>` en los 3 envíos (confirmación, cambio de estado, newsletter), y se documentaron las variables `SMTP_*` en `.env.example`. Issue cerrado aceptando esta mitigación: la solución completa (dominio propio + SPF/DKIM/DMARC + servicio transaccional dedicado) queda fuera de alcance por ahora — requiere compra/decisión aparte, no solo código.
