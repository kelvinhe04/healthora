# Clave publica de Clerk usada por el flujo de inicio de sesion del frontend.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me

# URL publica opcional de la API para futuros despliegues.
# En desarrollo local la app usa el proxy de Vite desde /api hacia http://localhost:3002.
# VITE_API_URL solo aplica si cambias api.ts a usarla; el proxy es /api por defecto.
VITE_API_URL=http://localhost:3002

# URL directa (wss://) del backend para el socket de notificaciones (HU-061). OBLIGATORIA en
# cualquier deploy detras de un rewrite tipo Vercel (ver vercel.json): esos rewrites proxean HTTP
# pero no upgrades de WebSocket, asi que el socket tiene que conectar directo al host del backend
# en vez de por /api. Sin esto cae a mismo-origen y falla en loop silenciosamente (ver #254).
# Alternativa: VITE_BACKEND_URL (https://...), se autoconvierte a wss://.
VITE_BACKEND_WS_URL=wss://tu-backend.example.com

# PostHog error tracking del frontend. Deja vacio para desactivar en desarrollo.
VITE_POSTHOG_PROJECT_TOKEN=phc_replace_me
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Cloud name de Cloudinary para CDN de imagenes: fetch-transform de URLs externas y delivery
# optimizado (f_auto,q_auto,w_) de las imagenes propias subidas/migradas a Cloudinary. Vacío =
# URLs sin optimizar/transformar. Mismo cloud name que CLOUDINARY_CLOUD_NAME en backend/.env.
VITE_CLOUDINARY_CLOUD_NAME=

# Clave publica de Stripe (HU-059, metodos de pago guardados) - usada solo para el formulario de
# tarjeta en el perfil (Stripe Elements + SetupIntent). El checkout en si sigue siendo la pagina
# hosteada de Stripe, no necesita esta clave. Mismo par que STRIPE_SECRET_KEY en backend/.env.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
